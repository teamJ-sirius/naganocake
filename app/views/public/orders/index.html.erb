<h1 class="text-center">注文履歴一覧</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">注文日</th>
      <th scope="col">配送先</th>
      <th scope="col">支払金額</th>
      <th scope="col">注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.created_at %></td>
        <td><%= order.address %></td>
        <td><%= order.total_price %></td>
        <td><%= order.order_status %></td>
      </tr>
    <% end %>
  </tbody>
</table>
