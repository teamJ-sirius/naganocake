<h1>ジャンル編集画面</h1>

<%= form_with model: @genre, url: admin_genre_path, local: true, method: :patch do |f| %>
  <div class="field">
    <%= f.text_field :name, autofocus: true %>
  </div>
  <%= f.submit "ジャンル名更新" %>
<% end %>

<%= link_to "戻る", admin_genres_path %>