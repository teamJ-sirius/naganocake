<div class="container">
  <h1>Customer List</h1>
  <table class="table table-bordered">
    <thead>
      <tr class="table-active">
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
        <% @customers.each do |customer| %>
          <tr>
            <td><%= customer.id %></td>
            <td><%= "#{customer.first_name}" + "#{customer.last_name}" %></td>
            <td><%= customer.email %></td>
            <td><% if customer.is_deleted == true %>
                  <%= "退会" %>
                 <% else %>
                  <%= "有効" %>
                 <% end %>
                 </td>
            <td><%= link_to "see more", admin_customer_path(customer.id) %> </td>
          </tr>
        <% end %>
    </tbody>
  </table>
</div>